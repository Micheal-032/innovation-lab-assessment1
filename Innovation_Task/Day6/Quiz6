1. What does filter() return?
It gives you a new array with only the items that match a certain condition.

2. How does reduce() work in JavaScript?
It goes through all items in an array and combines them into a single value (like a total or result).

3. Difference between slice() and splice()?
slice() copies part of an array without changing it, while splice() actually changes the original array.

4. What is the spread operator used for?
It helps to copy or combine arrays or objects easily using ....

5. How to define a method inside an object?
const person = { sayHi() { console.log("Hi"); } };

6. What does destructuring do?
It lets you pull values out of arrays or objects into separate variables quickly.