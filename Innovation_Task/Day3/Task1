<!DOCTYPE html>
<html>
<head>
  <title>Task 1: Playing with Functions</title>
</head>
<body>
  <h2>Task 1: Defining and Calling Functions</h2>

  <h4>Simple Greeting Function:</h4>
  <button onclick="greetUser()">Say Hello</button>

  <h4>Using <code>arguments</code> to Add Numbers:</h4>
  <button onclick="sumArguments(10, 20, 30)">Add 10, 20, 30</button>

  <h4>Using <code>...rest</code> to Add Numbers:</h4>
  <button onclick="sumRest(5, 15, 25, 35)">Add 5, 15, 25, 35</button>

  <p>Check the console (Right-click → Inspect → Console) to see what happens!</p>

  <script>
    // Simple function with no parameters
    function greetUser() {
      const message = "Hey there! Glad you're learning JavaScript";
      console.log(message);
    }

    // Using the 'arguments' object (old school but cool)
    function sumArguments() {
      let total = 0;
      for (let i = 0; i < arguments.length; i++) {
        total += arguments[i];
      }
      console.log("Total (with arguments object):", total);
    }

    // Using the rest operator (...) to accept any number of inputs
    function sumRest(...numbers) {
      const total = numbers.reduce((acc, curr) => acc + curr, 0);
      console.log("Total (with rest operator):", total);
    }
  </script>
</body>
</html>
